package assets;

class Paths {
    /*
    public static function getAsset<T:AssetType>(id:String, type:AssetType, shouldCache:Bool):T {
        if(shouldCache) {
            switch(type) {

            }
        }
    }
    */
}